---
"@ambiten/adapter-graphql": patch
---

Expose `createApolloAdapter()` and `createYogaAdapter()` from the package root.

The execution-scoped Apollo and GraphQL Yoga adapters introduced in 2.0.0 are now available through the documented `@ambiten/adapter-graphql` public entry point.